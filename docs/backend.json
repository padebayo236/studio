
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user account within the AgriPro Manager system, including administrative roles, farm managers, and farm workers who log in. Controls role-based access and permissions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity, corresponding to the Firebase Authentication user ID."
        },
        "name": {
          "type": "string",
          "description": "The full name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user, used for login.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The contact phone number of the user."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the system.",
          "enum": ["Admin", "FarmManager", "FarmWorker", "Accountant"]
        },
        "assignedFieldIds": {
          "type": "array",
          "description": "For managers/workers, an array of Field IDs they are assigned to.",
          "items": {
            "type": "string"
          }
        },
        "status": {
          "type": "string",
          "description": "The status of the user account.",
          "enum": ["active", "inactive"]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        }
      },
      "required": ["id", "name", "email", "role", "status", "createdAt"]
    },
    "Worker": {
      "title": "Worker",
      "type": "object",
      "description": "Represents a farm worker whose labor is managed and tracked by the system. Stores detailed farm labor records.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Worker entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User entity if this worker has a login account."
        },
        "managerId": {
          "type": "string",
          "description": "Reference to the User entity of the FarmManager for this worker."
        },
        "name": {
          "type": "string",
          "description": "The full name of the farm worker."
        },
        "age": {
          "type": "number",
          "description": "The age of the farm worker."
        },
        "gender": {
          "type": "string",
          "description": "The gender of the farm worker."
        },
        "phone": {
          "type": "string",
          "description": "The contact phone number of the farm worker."
        },
        "address": {
          "type": "string",
          "description": "The physical address of the farm worker."
        },
        "employmentType": {
          "type": "string",
          "description": "The employment type of the worker.",
          "enum": ["Permanent", "Seasonal", "Daily Wage", "Not Assigned"]
        },
        "wageRate": {
          "type": "number",
          "description": "The rate at which the worker is paid."
        },
        "assignedField": {
          "type": "string",
          "description": "Reference to the Field entity where this worker is primarily assigned."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL to the worker's profile photo.",
          "format": "uri"
        },
        "status": {
          "type": "string",
          "description": "The current employment status of the worker.",
          "enum": ["Active", "Inactive"]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the worker profile was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "employmentType",
        "wageRate",
        "status",
        "createdAt"
      ]
    },
    "Field": {
      "title": "Field",
      "type": "object",
      "description": "Represents a specific agricultural field or plot within the farm. Links tasks, productivity, and workers to specific farmland.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Field entity."
        },
        "name": {
          "type": "string",
          "description": "The name or identifier of the field."
        },
        "size": {
          "type": "number",
          "description": "The size of the field."
        },
        "sizeUnit": {
          "type": "string",
          "description": "The unit of measurement for the field size.",
          "enum": ["acres", "hectares"]
        },
        "cropType": {
          "type": "string",
          "description": "The primary crop type cultivated in this field."
        },
        "season": {
          "type": "string",
          "description": "The current or most recent planting season for this field."
        },
        "managerId": {
            "type": "string",
            "description": "The ID of the manager responsible for this field."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the field record was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "size",
        "sizeUnit",
        "cropType",
        "season",
        "createdAt"
      ]
    },
    "Task": {
      "title": "Task",
      "type": "object",
      "description": "Represents a specific task assigned to farm workers within a field. Tracks all operational farm activities.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Task entity."
        },
        "fieldId": {
          "type": "string",
          "description": "Reference to the Field where this task is to be performed."
        },
        "cropType": {
          "type": "string",
          "description": "The specific crop type related to this task."
        },
        "taskType": {
          "type": "string",
          "description": "The type of farm activity.",
          "enum": [
            "Planting",
            "Harvesting",
            "Weeding",
            "Irrigation",
            "Fertilizer Application"
          ]
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the task."
        },
        "expectedOutput": {
          "type": "number",
          "description": "The expected quantity of work to be completed."
        },
        "expectedOutputUnit": {
          "type": "string",
          "description": "The unit for the expected output."
        },
        "assignedWorkerIds": {
          "type": "array",
          "description": "References to Worker entities assigned to this task.",
          "items": {
            "type": "string"
          }
        },
        "status": {
          "type": "string",
          "description": "The current status of the task.",
          "enum": ["Pending", "In Progress", "Completed"]
        },
        "deadline": {
          "type": "string",
          "description": "The target date for task completion.",
          "format": "date-time"
        },
        "managerId": {
          "type": "string",
          "description": "The ID of the user who created the task."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the task was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "fieldId",
        "taskType",
        "assignedWorkerIds",
        "status",
        "deadline",
        "managerId",
        "createdAt"
      ]
    },
    "Attendance": {
      "title": "Attendance",
      "type": "object",
      "description": "Records the daily clock-in and clock-out for a farm worker. Calculates working hours and supports payroll.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Attendance entity."
        },
        "workerId": {
          "type": "string",
          "description": "Reference to the Worker whose attendance is being recorded."
        },
        "date": {
          "type": "string",
          "description": "The date of the attendance record.",
          "format": "date"
        },
        "timeIn": {
          "type": "string",
          "description": "The timestamp when the worker clocked in.",
          "format": "date-time"
        },
        "timeOut": {
          "type": "string",
          "description": "The timestamp when the worker clocked out.",
          "format": "date-time"
        },
        "totalHoursWorked": {
          "type": "number",
          "description": "The calculated total hours worked for the day."
        }
      },
      "required": [
        "id",
        "workerId",
        "date",
        "timeIn"
      ]
    },
    "Productivity": {
      "title": "Productivity",
      "type": "object",
      "description": "Captures the output or work completed by a worker for a specific task. Tracks farm efficiency and performance metrics.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Productivity entity."
        },
        "workerId": {
          "type": "string",
          "description": "Reference to the Worker who recorded this output."
        },
        "fieldId": {
          "type": "string",
          "description": "Reference to the Field where the output was achieved."
        },
        "taskId": {
          "type": "string",
          "description": "Reference to the Task under which this output was achieved."
        },
        "date": {
          "type": "string",
          "description": "The date when the productivity was recorded.",
          "format": "date"
        },
        "outputQuantity": {
          "type": "number",
          "description": "The quantity of output recorded."
        },
        "outputUnit": {
          "type": "string",
          "description": "The unit of measurement for the output quantity."
        },
         "hoursWorkedForEntry": {
          "type": "number",
          "description": "The hours spent to achieve this output."
        }
      },
      "required": [
        "id",
        "workerId",
        "fieldId",
        "taskId",
        "date",
        "outputQuantity",
        "outputUnit",
        "hoursWorkedForEntry"
      ]
    },
    "Payroll": {
      "title": "Payroll",
      "type": "object",
      "description": "Contains the calculated payroll for a worker for a specific month. Automates labor cost management.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Payroll entity."
        },
        "workerId": {
          "type": "string",
          "description": "Reference to the Worker for whom this payroll is generated."
        },
        "month": {
          "type": "number",
          "description": "The month for which the payroll is calculated (1-12)."
        },
        "year": {
          "type": "number",
          "description": "The year for which the payroll is calculated."
        },
        "totalHoursWorkedMonth": {
          "type": "number",
          "description": "The total hours worked by the worker in this month."
        },
        "totalTaskBasedPayment": {
          "type": "number",
          "description": "Total payment calculated based on task output."
        },
        "totalPaymentDue": {
          "type": "number",
          "description": "The total payment due to the worker for the month."
        },
        "generatedAt": {
          "type": "string",
          "description": "Timestamp when this payroll summary was generated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "workerId",
        "month",
        "year",
        "totalPaymentDue",
        "generatedAt"
      ]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
      "/users/{userId}": {
        "schema": { "$ref": "#/entities/User" },
        "description": "Stores user account information and roles for access control."
      },
      "/workers/{workerId}": {
        "schema": { "$ref": "#/entities/Worker" },
        "description": "Contains detailed profiles for all farm workers."
      },
      "/attendance/{attendanceId}": {
        "schema": { "$ref": "#/entities/Attendance" },
        "description": "Records the daily clock-in and clock-out for a farm worker."
      },
      "/productivity/{productivityId}": {
        "schema": { "$ref": "#/entities/Productivity" },
        "description": "Captures the output or work completed by a worker for a specific task."
      },
      "/payroll/{payrollId}": {
        "schema": { "$ref": "#/entities/Payroll" },
        "description": "Contains the calculated payroll for a worker for a specific month."
      },
      "/fields/{fieldId}": {
        "schema": { "$ref": "#/entities/Field" },
        "description": "Stores information about specific farm fields or plots."
      },
      "/tasks/{taskId}": {
        "schema": { "$ref": "#/entities/Task" },
        "description": "Defines and tracks all operational farm activities."
      }
  }
}
